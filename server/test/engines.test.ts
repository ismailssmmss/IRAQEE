import {describe,it,expect} from 'vitest'; import {zigzag} from '../src/zigzag'; import {scan} from '../src/harmonics';
const candles=(prices:number[])=>prices.map((p,i)=>({time:i,open:p,high:p+1,low:p-1,close:p}));
describe('ZigZag',()=>{it('alternates and leaves latest leg provisional',()=>{const s=zigzag(candles([10,10,12,12,8,8,13,13,11]),{deviation:10,depth:1,backstep:1,tolerance:.05});expect(s.some(x=>x.type==='HIGH')).toBe(true);expect(s.at(-1)?.confirmed).toBe(false)});it('needs depth history',()=>expect(zigzag(candles([1,2]),{deviation:1,depth:2,backstep:1,tolerance:.05})).toEqual([]))});
describe('Harmonics',()=>{it('rejects wrong ordering',()=>expect(scan([{index:0,time:0,price:1,type:'LOW',confirmed:true},{index:1,time:1,price:2,type:'HIGH',confirmed:true},{index:2,time:2,price:3,type:'HIGH',confirmed:true},{index:3,time:3,price:2,type:'LOW',confirmed:true},{index:4,time:4,price:3,type:'HIGH',confirmed:true}])).toHaveLength(0));it('returns mathematical candidates only',()=>{const p=[{index:0,time:0,price:100,type:'LOW',confirmed:true},{index:1,time:1,price:120,type:'HIGH',confirmed:true},{index:2,time:2,price:107.64,type:'LOW',confirmed:true},{index:3,time:3,price:114,type:'HIGH',confirmed:true},{index:4,time:4,price:104.28,type:'LOW',confirmed:true}] as any;expect(scan(p).every(x=>x.ratios.AB_XA>0)).toBe(true)})});
